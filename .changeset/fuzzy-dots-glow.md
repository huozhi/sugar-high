---
"sugar-high": minor
"@sugar-high/react": minor
---

Add opt-in WebGPU syntax highlighting through `sugar-high/gpu` and client-side `Code` and `Editor` components from `@sugar-high/react/gpu`. The experimental `gpu-lexer` integration is an optional peer dependency accepting version 0.0.2 and newer, and is installed separately.
