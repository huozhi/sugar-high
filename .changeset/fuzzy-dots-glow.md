---
"sugar-high": minor
"@sugar-high/react": minor
---

Add opt-in WebGPU syntax highlighting through `sugar-high/gpu` and client-side `Code` and `Editor` components from `@sugar-high/react/gpu`. The experimental `gpu-lexer@0.0.2` integration is an optional peer dependency and is installed separately.
