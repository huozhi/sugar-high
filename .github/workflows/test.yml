name: Test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: pnpm/action-setup@v2.1.0
      with:
          version: 6.11.0
      - run: pnpm install
      - run: pnpm test
        env:
          CI: true